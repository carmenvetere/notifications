{
  "issues": {
    "delivery_failed": {
      "title": "Notification delivery failed",
      "description": "A notification couldn't be delivered via `{service}`: {error}.\n\nCheck that the service exists and its targets are valid (e.g. the mobile app notify service name)."
    },
    "template_error": {
      "title": "Notification rule template error",
      "description": "Rule **{rule}** has a template that failed to render:\n\n```\n{error}\n```\n\nFix the template in the Notification Center panel."
    },
    "no_mobile_targets": {
      "title": "No mobile notify targets configured",
      "description": "A rule tried to send an app (mobile) notification, but no mobile notify services are configured, so the push was silently skipped.\n\nOpen **Settings → Devices & Services → Notification Center → Configure** and set **Mobile notify services** (e.g. `notify.mobile_app_yourphone`), or add presence-mapped people. Then run the `notification_center.test_push` service to verify."
    },
    "action_failed": {
      "title": "Notification action failed",
      "description": "A notification's custom action couldn't run `{service}`: {error}.\n\nCheck that the service/script exists and its name is correct (e.g. `script.reset_vacuum`). The alert was kept so you can retry."
    }
  },
  "config": {
    "step": {
      "user": {
        "title": "Notification Center",
        "description": "Set up the Notification Center. After adding it, configure routing under Options and add notification rules from the Notifications panel in the sidebar."
      }
    },
    "abort": {
      "already_configured": "Notification Center is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Routing & behavior",
        "data": {
          "mobile_targets": "Mobile notify services (e.g. notify.mobile_app_phone)",
          "persons": "Presence-mapped people — each with person, notify, media_player",
          "tts_service": "TTS service",
          "tts_default_targets": "Default TTS media players",
          "fully_kiosk_devices": "Fully Kiosk device IDs (for force-navigate)",
          "quiet_hours_start": "Quiet hours start",
          "quiet_hours_end": "Quiet hours end",
          "digest_time": "Daily digest delivery time",
          "debounce_ms": "Re-evaluation debounce (ms)"
        }
      }
    }
  },
  "services": {
    "send": {
      "name": "Send notification",
      "description": "Create a one-off notification, routed by priority and channels."
    },
    "dismiss": {
      "name": "Dismiss",
      "description": "Remove an active alert and clear its bell notification."
    },
    "dismiss_item": {
      "name": "Dismiss digest item",
      "description": "Hide a single item within a digest alert (by its name/key)."
    },
    "snooze": {
      "name": "Snooze",
      "description": "Dismiss an alert and suppress it for a number of minutes."
    },
    "run_action": {
      "name": "Run custom action",
      "description": "Run a rule-defined custom action (e.g. a reset script) and clear the alert."
    },
    "reload": {
      "name": "Reload",
      "description": "Rebuild rules and listeners without restarting Home Assistant."
    },
    "import_rules": {
      "name": "Import rules",
      "description": "Bulk-create rule subentries from the packaged file or an inline list (idempotent)."
    },
    "test_push": {
      "name": "Test app notification",
      "description": "Send a test push to every configured mobile notify target to verify app notifications work."
    }
  }
}
