# Test dependencies. pytest-homeassistant-custom-component pins a matching
# Home Assistant release, so we do NOT install `homeassistant` separately.
# Pin this to track the HA version you run in production.
# Pinned so CI is reproducible and tracks a known HA version. This release
# supports Python 3.13 (matching the CI job) and pins homeassistant==2026.2.3.
# Newer 0.13.x releases require Python >=3.14 — bump the CI Python and this pin
# together when you move your production HA forward. Version map:
# https://github.com/MatthewFlamm/pytest-homeassistant-custom-component/blob/master/README.md
pytest-homeassistant-custom-component==0.13.316
# Time control for deferred-delivery (digest / quiet-hours batch) flush tests.
# Ships transitively with HA, but pinned here so the import is guaranteed.
freezegun
