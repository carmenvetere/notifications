[pytest]
asyncio_mode = auto
testpaths = tests
# Pure-logic tests are test_*.py (also runnable via stdlib unittest without HA);
# the HA integration tests are integration_*.py.
python_files = test_*.py integration_*.py
